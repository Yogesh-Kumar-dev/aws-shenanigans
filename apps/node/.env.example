STAGE=dev
AWS_REGION=ap-south-2
LOG_LEVEL=INFO

# Set automatically by serverless.yml from the service/stage, but you can
# override it for `serverless invoke local` against a specific table.
# TASKS_TABLE=crud-api-dev-tasks
